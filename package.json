{
  "scripts": {
    "start": "npm-run-all --parallel start:server start:web",
    "start:web": "cd client && yarn start",
    "start:server": "cd backend && yarn server"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {}
}
